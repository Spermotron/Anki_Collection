To update Anki Collections, use the File>Import tool in your Anki application and
select the backup-*<most recent backup>.colpkg for importing. 
Note this will overwrite your current collection with whatever is in this one!
